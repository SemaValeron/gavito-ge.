// "Мозг" нашего ИИ с рыночными данными
  const getGlobalMarketAdvice = (price, title, cat) => {
    if (!price || !title) return null;
    const p = parseFloat(price);
    const name = title.toLowerCase();

    // База средних цен (Магазины и Б/У рынок)
    const MARKET_DATA = {
      'iphone 17 pro max': { new: 4500, used: 3800 },
      'iphone 15': { new: 2200, used: 1600 },
      'macbook': { new: 3500, used: 2500 },
      'playstation 5': { new: 1500, used: 1100 },
      'bmw': { used_avg: 15000 }
    };

    // Поиск совпадения в базе
    let itemData = null;
    for (let key in MARKET_DATA) {
      if (name.includes(key)) {
        itemData = MARKET_DATA[key];
        break;
      }
    }

    // Если товар найден в "памяти" ИИ
    if (itemData) {
      const marketPrice = itemData.new || itemData.used_avg;
      
      if (p < marketPrice * 0.1) {
        return `⚠️ ყურადღება! ეს ფასი (${p}₾) ზედმეტად დაბალია. ბაზარზე ეს ნივთი ~${marketPrice}₾ ღირს. შესაძლოა თაღლითობაა ან ნივთი დაზიანებულია.`;
      }
      if (p > marketPrice * 1.3) {
        return `📈 ფასი ძალიან მაღალია. მაღაზიებში ახალი ღირს ~${marketPrice}₾. გირჩევთ დააკლოთ, რომ სწრაფად გაყიდოთ.`;
      }
      if (p < marketPrice * 0.8) {
        return `✅ იდეალური ფასია! მეორადი ბაზრისთვის ეს ძალიან კონკურენტული შეთავაზებაა.`;
      }
      return `🔹 ფასი შეესაბამება ბაზრის საშუალო მაჩვენებელს (~${marketPrice}₾).`;
    }

    // Если товара нет в базе, используем общую логику по категориям
    if (cat === 'tech' && p < 10) return "⚠️ ფასი არარეალურია ტექნიკისთვის. შეამოწმეთ ციფრები.";
    if (p > 10000 && cat !== 'cars' && cat !== 'realestate') return "📈 ეს ფასი ძალიან მაღალია ამ კატეგორიისთვის.";
    
    return "🔍 ბაზრის ანალიზი: ფასი მისაღებია, მსგავსი განცხადებები სხვა საიტებზეც ამ დიაპაზონშია.";
  };
